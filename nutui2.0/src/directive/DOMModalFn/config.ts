import { MPopup } from '../../components'
import CommonModal from './pools/CommonModal'

const config = [
  {
    name: 'common',
    component: CommonModal
  }
]

const mpopConfig = [
  {
    name: 'mpopup',
    component: MPopup
  }
]

export default {
  config,
  mpopConfig
}
